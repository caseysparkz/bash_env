###: Export encrypted environment variables to bash session
getsecrets() {
	for TOKEN in $(ls ${HOME}/.env/secrets); do
		for LINE in $(gpg --decrypt ${HOME}/.env/secrets/${TOKEN} 2> /dev/null); do
			export ${LINE}
		done
	done
}
