### upgr: distro-agnostic package upgrader.

upgr() {

	if [ -f /bin/pacman ]; then
		sudo pacman -Syu --noconfirm
	
	elif [ -f /bin/apt ]; then
		sudo apt upgrade -y

	elif [ -f /bin/dnf ]; then
		sudo dnf upgrade -y

	fi

}
