### install: distro-agnostic package installer.

install() {

	if [ -x /bin/pip ]; then
    sudo pip install $@

  elif [ -x /bin/pacman ]; then
		sudo pacman -Syu $@ --noconfirm
	
	elif [ -x /bin/apt ]; then
		sudo apt install $@ -y

	elif [ -x /bin/dnf]; then
		sudo dnf install $@ -y

	fi

}
